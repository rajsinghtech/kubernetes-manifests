---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Intentionally no top-level namespace: the restic orphan cleanup Roles live
# in home/media, while the Job/SA live in garage. The PV is cluster-scoped.
resources:
  - pv.yaml
  - restic-orphan-cleanup-rbac.yaml
  - restic-orphan-cleanup-job.yaml
