---
# Qwen3.8-Flash-Next-NVFP4 OpenAI-compatible inference server.
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: app-vllm
spec:
  jobName: blackbox
  module: http_2xx
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - http://qwen38.ai:8000/health
        - http://qwen38.ai:8000/v1/models
      labels:
        service: ai-inference
        target: vllm
