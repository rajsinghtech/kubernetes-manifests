---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kro
  namespace: kro-system
spec:
  interval: 1m
  chart:
    spec:
      chart: kro
      version: 0.9.4
      interval: 5m
      sourceRef:
        kind: HelmRepository
        name: kro
        namespace: flux-system
