---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  # name: kbench-pvc-block-replicated
  name: kbench-pvc-block-optimized
spec:
  volumeMode: Filesystem
  # storageClassName: ceph-block-replicated
  storageClassName: ceph-block-optimized
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi