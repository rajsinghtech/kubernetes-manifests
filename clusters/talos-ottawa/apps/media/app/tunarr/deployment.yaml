apiVersion: apps/v1
kind: Deployment
metadata:
  name: tunarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tunarr
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: tunarr
    spec:
      containers:
        - name: tunarr
          image: chrisbenincasa/tunarr:1.0.3
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          volumeMounts:
            - name: config-volume
              mountPath: /config
          env:
            - name: PUID
              value: "0"
            - name: GUID
              value: "0"
            - name: TZ
              value: ${TIMEZONE}
          resources:
            requests:
              gpu.intel.com/i915: "25"
            limits:
              gpu.intel.com/i915: "25"
      volumes:
        - name: config-volume
          persistentVolumeClaim:
            claimName: tunarr-config
