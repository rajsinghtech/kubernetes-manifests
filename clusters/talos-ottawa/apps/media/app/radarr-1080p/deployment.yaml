apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr-1080p
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radarr-1080p
  template:
    metadata:
      labels:
        app: radarr-1080p
    spec:
      strategy:
        type: Recreate
      containers:
      - name: radarr-1080p
        image: lscr.io/linuxserver/radarr:latest
        ports:
        - name: http
          containerPort: 7878
          protocol: TCP
        volumeMounts:
        - name: movies-volume
          mountPath: /movies
        - name: downloads-volume
          mountPath: /downloads
        - name: config-volume
          mountPath: /config
        env:
        - name: PUID
          value: "0"
        - name: GUID
          value: "0"
        - name: TZ
          value: ${TIMEZONE}
      volumes:
      - name: movies-volume
        persistentVolumeClaim:
          claimName: radarr-1080p-movies
      - name: downloads-volume
        persistentVolumeClaim:
          claimName: sabnzbd-media
      - name: config-volume
        persistentVolumeClaim:
          claimName: radarr-1080p-config 