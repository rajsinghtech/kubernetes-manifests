apiVersion: v1
kind: Secret
metadata:
  name: qbittorrent-pvt-secret
type: Opaque
stringData:
  WIREGUARD_ENDPOINT_IP: ${QB_PVT_WIREGUARD_ENDPOINT_IP}
  WIREGUARD_PUBLIC_KEY: ${QB_PVT_WIREGUARD_PUBLIC_KEY}
  WIREGUARD_PRIVATE_KEY: ${QB_PVT_WIREGUARD_PRIVATE_KEY}
  WIREGUARD_ADDRESSES: ${QB_PVT_WIREGUARD_ADDRESSES}
  UPDATER_PROTONVPN_EMAIL: ${QB_PROTONVPN_EMAIL}
  UPDATER_PROTONVPN_PASSWORD: ${QB_PROTONVPN_PASSWORD}