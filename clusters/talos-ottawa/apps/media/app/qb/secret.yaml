apiVersion: v1
kind: Secret
metadata:
  name: qbittorrent-secret
type: Opaque
stringData:
  WIREGUARD_ENDPOINT_IP: ${COMMON_WIREGUARD_ENDPOINT_IP}
  WIREGUARD_PUBLIC_KEY: ${COMMON_WIREGUARD_PUBLIC_KEY}
  WIREGUARD_PRIVATE_KEY: ${COMMON_WIREGUARD_PRIVATE_KEY}
  WIREGUARD_ADDRESSES: ${COMMON_WIREGUARD_ADDRESSES}