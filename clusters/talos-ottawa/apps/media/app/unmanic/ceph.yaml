apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unmanic-config-ceph
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block-replicated-nvme