apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unmanic-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: unmanic-config