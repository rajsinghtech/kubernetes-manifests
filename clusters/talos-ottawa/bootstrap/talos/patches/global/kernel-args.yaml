machine:
  install:
    extraKernelArgs:
      - intel_iommu=on
      - iommu=pt
      - mitigations=off
      - net.ifnames=0
      - talos.platform=metal
      - security=none
      - cpufreq.default_governor=performance
      - intel_idle.max_cstate=0
      - -selinux