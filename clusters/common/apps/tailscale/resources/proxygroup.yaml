apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: common-egress-proxy
spec:
  hostnamePrefix: ${LOCATION}-egress-proxy
  # tags:
  #   - egress
  proxyClass: common
  type: egress
  replicas: 3
---
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: common-ingress-proxy
spec:
  hostnamePrefix: ${LOCATION}-ingress-proxy
  # tags:
  #   - ingress
  proxyClass: common
  type: ingress
  replicas: 3