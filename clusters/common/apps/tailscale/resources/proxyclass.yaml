apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: tailscale-egress-proxy
spec:
  tailscale:
    acceptRoutes: true
  metrics:
    enable: true
    serviceMonitor:
      enable: true