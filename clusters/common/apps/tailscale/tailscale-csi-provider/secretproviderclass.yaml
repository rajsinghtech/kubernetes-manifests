apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: tailscale
spec:
  provider: tailscale
  parameters:
    authKey: '{"ephemeral":true,"reusable":false,"preauthorized":true,"tags":["tag:k8s"]}'
