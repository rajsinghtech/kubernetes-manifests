apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: ts-pihole-udp
spec:
  parentRefs:
    - name: ts
      sectionName: pihole-udp
  rules:
    - backendRefs:
        - name: ts-pihole-dns
          port: 53