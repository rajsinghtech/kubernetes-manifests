apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tls-redirect
  namespace: home
spec:
  parentRefs:
    - name: cdn
      sectionName: wildcard-cdn-${COMMON_DOMAIN//./-}-http
    - name: cdn
      sectionName: wildcard-${LOCATION}-${COMMON_DOMAIN//./-}-http
    - name: ${LOCATION}
      sectionName: wildcard-cdn-${COMMON_DOMAIN//./-}-http
    - name: ${LOCATION}
      sectionName: wildcard-${LOCATION}-${COMMON_DOMAIN//./-}-http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
