---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns-cdn
spec:
  interval: 5m
  releaseName: external-dns-cdn
  chart:
    spec:
      chart: external-dns
      version: "1.19.0" 
      sourceRef:
        kind: HelmRepository
        name: external-dns
        namespace: flux-system 
      interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    provider: pihole
    env:
      - name: EXTERNAL_DNS_PIHOLE_PASSWORD
        valueFrom:
          secretKeyRef:
            name: pihole
            key: "password"
    extraArgs:
      - --ignore-ingress-tls-spec
      - --pihole-server=http://pihole-cdn-web
      - --gateway-label-filter=external-dns==cdn
      - --pihole-api-version=6
    policy: sync
    sources:
      - gateway-httproute
      - gateway-tlsroute
      - gateway-tcproute
      - gateway-udproute
    domainFilters: 
      - ${COMMON_DOMAIN}
    txtOwnerId: cdn-${COMMON_DOMAIN//./-}
    txtPrefix: k8s.cdn.${COMMON_DOMAIN}