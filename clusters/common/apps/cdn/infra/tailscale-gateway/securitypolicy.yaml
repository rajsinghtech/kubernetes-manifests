  apiVersion: gateway.envoyproxy.io/v1alpha1
  kind: SecurityPolicy
  metadata:
    name: allow-cgnat-range
    namespace: envoy-gateway-system
  spec:
    targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: cdn  
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: ${LOCATION}  
    authorization:
      defaultAction: Allow