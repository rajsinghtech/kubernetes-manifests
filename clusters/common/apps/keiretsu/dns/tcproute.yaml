apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: pihole-tcp
spec:
  parentRefs:
  - name: ts
    sectionName: pihole-tcp
  rules:
  - backendRefs:
    - name: pihole-dns
      port: 53