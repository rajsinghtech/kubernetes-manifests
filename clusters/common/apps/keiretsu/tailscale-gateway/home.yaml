apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: home
spec:
  endpoints:
    - fqdn:
        hostname: dashboard-ts-homer.keiretsu.ottawa.k8s
        port: 80
    - fqdn:
        hostname: dashboard-ts-homer.keiretsu.robbinsdale.k8s
        port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: home
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: ts
      namespace: keiretsu
  hostnames:
    - "home.${COMMON_DOMAIN}"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: home
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /