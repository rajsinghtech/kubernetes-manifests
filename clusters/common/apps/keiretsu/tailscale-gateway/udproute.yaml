apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: pihole-udp
spec:
  parentRefs:
    - name: ts
      sectionName: pihole-udp
  rules:
    - backendRefs:
        - name: pihole-dns
          port: 53