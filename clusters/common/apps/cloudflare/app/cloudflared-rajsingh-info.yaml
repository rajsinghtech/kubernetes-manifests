apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cloudflared-rajsingh-info
  name: cloudflared-rajsingh-info
  namespace: cloudflared
spec:
  replicas: 3
  selector:
    matchLabels:
      pod: cloudflared-rajsingh-info
  template:
    metadata:
      labels:
        pod: cloudflared-rajsingh-info
    spec:
      containers:
        - command:
            - cloudflared
            - tunnel
            - --metrics
            - 0.0.0.0:2000
            - run
          args:
            - --token
            - $(token)
          image: cloudflare/cloudflared:latest
          name: cloudflared
          env:
            - name: token
              valueFrom:
                secretKeyRef:
                  name: cloudflare
                  key: "RAJSINGH_INFO_CLOUDFLARED_TOKEN"
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 1
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            limits:
              cpu: "0.5"
              memory: "256Mi"
            requests:
              cpu: "0.2"
              memory: "128Mi"
      # dnsPolicy: None
      # dnsConfig:
      #   nameservers:
      #     - 10.50.10.50
      #     - 10.50.69.10
      #   options:
      #     - name: ndots
      #       value: "2"
