apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns-lukehouge-com
  labels:
    app: cloudflare-ddns-lukehouge-com
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-ddns-lukehouge-com
  template:
    metadata:
      labels:
        app: cloudflare-ddns-lukehouge-com
    spec:
      containers:
        - name: cloudflare-ddns
          image: favonia/cloudflare-ddns:latest
          env:
            - name: CLOUDFLARE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare
                  key: LUKEHOUGE_COM_CLOUDFLARE_API_TOKEN
            - name: DOMAINS
              value: "${LOCATION}.lukehouge.com"
            - name: PROXIED
              value: "false"
            - name: IP6_PROVIDER
              value: "none"
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
