apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns-rajsingh-info
  labels:
    app: cloudflare-ddns-rajsingh-info
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-ddns-rajsingh-info
  template:
    metadata:
      labels:
        app: cloudflare-ddns-rajsingh-info
    spec:
      containers:
        - name: cloudflare-ddns
          image: favonia/cloudflare-ddns:latest
          env:
            - name: CLOUDFLARE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare
                  key: RAJSINGH_INFO_CLOUDFLARE_API_TOKEN
            - name: DOMAINS
              value: "${LOCATION}.rajsingh.info"
            - name: PROXIED
              value: "false"
            - name: IP6_PROVIDER
              value: "none"
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
