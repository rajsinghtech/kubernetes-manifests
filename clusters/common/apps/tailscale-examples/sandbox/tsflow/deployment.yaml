apiVersion: apps/v1
kind: Deployment
metadata:
  name: tsflow
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tsflow
  template:
    metadata:
      labels:
        app: tsflow
    spec:
      containers:
        - name: tsflow
          image: ghcr.io/rajsinghtech/tsflow:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000 
              protocol: TCP
          env:
            - name: TAILSCALE_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tsflow
                  key: TAILSCALE_ACCESS_TOKEN
            - name: TAILSCALE_TAILNET
              valueFrom:
                secretKeyRef:
                  name: tsflow
                  key: TAILSCALE_TAILNET