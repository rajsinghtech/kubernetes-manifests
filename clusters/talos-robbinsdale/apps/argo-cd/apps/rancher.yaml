apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rancher
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: cattle-system
  project: default
  source:
    path: clusters/talos-robbinsdale/apps/rancher
    repoURL: https://github.com/rajsinghtech/kubernetes-manifests
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true