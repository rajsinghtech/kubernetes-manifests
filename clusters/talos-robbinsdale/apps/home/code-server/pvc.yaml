apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code-server-config
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: smb	
  volumeMode: Filesystem
  volumeName: code-server-config