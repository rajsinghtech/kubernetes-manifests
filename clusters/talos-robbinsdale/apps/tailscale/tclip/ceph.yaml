apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tclip-data-ceph
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ceph-block-replicated-nvme 