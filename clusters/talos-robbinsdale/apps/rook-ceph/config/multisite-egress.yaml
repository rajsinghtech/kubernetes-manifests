apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-rgw-object-store
  annotations:
    # tailscale.com/tailnet-ip: 100.89.67.122
    tailscale.com/tailnet-fqdn: rook-ceph-rook-ceph-rgw-object-store.keiretsu.ts.net
    # tailscale.com/proxy-group: common-egress
spec:
  externalName: placeholder
  type: ExternalName
  ports:
  - name: http
    port: 80
    protocol: TCP