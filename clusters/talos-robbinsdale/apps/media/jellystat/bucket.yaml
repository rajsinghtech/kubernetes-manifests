apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: jellystat-postgres-ceph-bucket
spec:
  generateBucketName: postgres
  bucketName: postgres
  storageClassName: rook-ceph-bucket