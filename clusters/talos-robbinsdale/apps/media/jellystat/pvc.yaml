---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellystat-backup-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-truenas
  # storageClassName: smb
  # volumeName: jellystat-backup-pvc-unas
