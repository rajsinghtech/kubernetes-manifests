apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config-ceph
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block-replicated-nvme 