apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: rook-public-nw
  namespace: rook-ceph
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "public-nad",
    "type": "macvlan",
    "mode": "bridge",
    "ipam": {
      "type": "whereabouts",
      "range": "10.2.1.0/24"
    }
  }'
---
apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: rook-cluster-nw
  namespace: rook-ceph
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "cluster-nad",
    "type": "macvlan",
    "mode": "bridge",
    "ipam": {
      "type": "whereabouts",
      "range": "10.2.2.0/24"
    }
  }'
