apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: ${APP}-${LOCATION}-udp
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "${APP}.${LOCATION}.${COMMON_DOMAIN},${APP}.cdn.${COMMON_DOMAIN}"
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: ${LOCATION}
      namespace: cdn
      sectionName: ${APP}-udp
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: ${APP}
          port: ${APP_PORT}
          weight: 1
---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: ${APP}-cdn-udp
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "${APP}.${LOCATION}.${COMMON_DOMAIN},${APP}.cdn.${COMMON_DOMAIN}"
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: cdn
      namespace: cdn
      sectionName: ${APP}-udp
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          port: ${APP_PORT}
          name: ${APP}-${LOCATION}-udp
          weight: 0
        - group: gateway.envoyproxy.io
          port: ${APP_PORT}
          name: ${APP}-${FAILOVER}-udp
          weight: 1