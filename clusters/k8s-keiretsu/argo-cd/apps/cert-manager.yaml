apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager-prd
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  project: default
  source:
    path: clusters/k8s-keiretsu/apps/cert-manager
    repoURL: https://github.com/rajsinghtech/kubernetes-manifests
    targetRevision: stg
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
