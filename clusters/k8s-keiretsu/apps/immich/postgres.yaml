apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-postgres
  namespace: immich
spec:
  instances: 3

  storage:
    size: 10Gi
    storageClass: nfs-proxmox-vault

  monitoring:
    enablePodMonitor: true