image:
  tag: v1.120.1
# env:
# - name: DB_DATABASE_NAME
#   value: immich
# - name: DB_HOSTNAME
#   valueFrom:
#     secretKeyRef:
#       name: immich-postgresql-app
#       key: host
# - name: DB_PASSWORD
#   valueFrom:
#     secretKeyRef:
#       name: immich-postgresql-app
#       key: password
# - name: DB_USERNAME
#   valueFrom:
#     secretKeyRef:
#       name: immich-postgresql-app
#       key: username
# - name: REDIS_HOSTNAME
#   value: immich-redis-headless
immich:
  metrics:
    # Enabling this will create the service monitors needed to monitor immich with the prometheus operator
    enabled: false
  persistence:
    library:
      existingClaim: immich

# # Dependencies
postgresql:
  enabled: true
  global:
    defaultStorageClass: nfs-proxmox-vault

redis:
  enabled: true
  global:
    defaultStorageClass: nfs-proxmox-vault
