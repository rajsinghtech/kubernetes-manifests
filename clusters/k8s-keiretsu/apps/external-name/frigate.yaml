apiVersion: v1
kind: Service
metadata:
  namespace: external-name
  name: frigate-svc
spec:
  externalName: frigate.local
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: 5000
  sessionAffinity: None
  type: ExternalName
