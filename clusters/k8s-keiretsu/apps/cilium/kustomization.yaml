apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - templates/bgp.yaml
  - templates/lbippool.yaml
helmCharts:
  - name: cilium
    repo: https://helm.isovalent.com
    version: v1.16.3-cee.1
    namespace: kube-system
    releaseName: cilium
    includeCRDs: true
    valuesFile: values.yaml
    apiVersions:
      - monitoring.coreos.com/v1

