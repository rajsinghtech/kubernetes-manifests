apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: transmission-movies-tls
  namespace: media
  annotations:
    cert-manager.io/cluster-issuer: global-issuer
    kubernetes.io/tls-acme: 'true'
spec:
  ingressClassName: haproxy
  rules:
  - host: transmission-movies.${INGRESS_DOMAIN}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: transmission-movies
            port:
              number: 9091
  tls:
  - secretName: transmission-movies-tls
    hosts:
    - transmission-movies.${INGRESS_DOMAIN}