apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tesla-tls
spec:
  dnsNames:
    - tesla.rajsingh.info
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: raj-issuer
  secretName: telsa-rajsingh-tls
  usages:
    - digital signature
    - key encipherment
