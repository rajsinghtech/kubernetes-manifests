apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-data-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3000Gi
  storageClassName: nfs-proxmox-vault
