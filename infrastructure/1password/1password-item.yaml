apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: proxmox-api-token  # Ensure this is lowercase and only uses valid characters
spec:
  itemPath: "vaults/K8s/items/Proxmox API Token"