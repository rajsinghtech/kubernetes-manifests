---
apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: hubble
spec:
  gatewayClassName: cilium
  listeners:
  - protocol: HTTP
    port: 80
    name: web-gw
    allowedRoutes:
      namespaces:
        from: All
---
apiVersion: v1
kind: Service
metadata:
  name: cilium-gateway
  namespace: kube-system
spec:
  ports:
  - name: port-80
    port: 80
    protocol: TCP
    targetPort: 80
  type: LoadBalancer