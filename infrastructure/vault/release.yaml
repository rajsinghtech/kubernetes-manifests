---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: vault
spec:
  interval: 2m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: vault
      version: x
      sourceRef:
        kind: HelmRepository
        name: hashicorp
  values:
    ui:
      enabled: true
    ha:
      enabled: true
      replicas: 3
    server:
      dataStorage:
        storageClass: nfs-client
