---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: argo-cd
spec:
  interval: 2m
  chart:
    spec:
      chart: argo-cd
      version: x
      sourceRef:
        kind: HelmRepository
        name: argocd
      valuesFiles:
        - values.yaml