name: Tailscale Status Check

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      RUNNER_DEBUG: 1
    steps:
      - name: Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: TbqNGJkY5611CNTRL
          audience: kb8cB2pMCZ11CNTRL
          tags: tag:k8s
          args: --accept-routes --accept-dns

      - name: Status
        run: tailscale status
